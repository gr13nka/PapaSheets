# Чек-лист ручной проверки на реальном устройстве

Эмулятор не умеет мультитач (пинч) и идёт на software-GPU (~2–3 fps), поэтому перф- и жест-гейты
проверяются только на настоящем телефоне. Ставим release-APK (`app-release.apk`, подписан) и проходим
по списку. Для нагрузочных пунктов засеять журнал «Январь 2000» (28×~300): долгое нажатие на FAB «+»
в debug-сборке; в release нагрузочного сидера нет — используем реальный месяц или заранее
импортированный бэкап.

## Матрица (LOD/зум) — M4
- [ ] **Пинч-зум-аут** до «картины месяца»: два пальца свести → плавный уход в heatmap LOD2, весь месяц
      виден одним экраном, шапка-подрядчики читается (вертикальные shortName), колонка дат — числа.
- [ ] **Пинч-зум-ин** обратно к ячейкам с фото — точка под пальцами не «уезжает».
- [ ] **Double-tap** по ячейке: анимированный тумблер обзор ↔ 1:1, фокус в точке тапа.
- [ ] Кнопка **«Месяц» ↔ «1:1»** в шапке делает то же, что double-tap.
- [ ] **~60 fps** на живом журнале 28×~300 при пинче/скролле/fling (Developer Options → Profile GPU
      Rendering, полосы под зелёной линией; либо `adb shell dumpsys gfxinfo ru.papasheets`).
- [ ] Тап по heatmap-ячейке открывает **правильную** запись (дата/подрядчик совпадают со столбцом/строкой).

## Лайтбокс — M2
- [ ] Тап по фото в ячейке → полноэкранный лайтбокс, **пинч-зум и панорамирование** фото плавные.

## Камера и прерывание — M2, риск 5
- [ ] Снять фото из формы записи (системная камера) → возвращается, вписывается в ячейку.
- [ ] **Прерывание камеры**: пока открыта камера, свернуть/убить процесс (Developer Options → «Don't
      keep activities» ВКЛ) → вернуться; форма не теряет уже введённые поля, temp-uri не протух, фото
      импортируется либо чисто отменяется (без битой ссылки).

## Форма записи — сохранение и сворачивание
- [ ] Новая запись: выбрать подрядчика, заполнить только «Локацию» (обязательный «Вид работ» пуст) →
      **свайп вниз** → запись появилась в матрице/списке, а внизу экрана осталась **полоска**
      «Запись свёрнута» с подписью вида «ГП · 1-01».
- [ ] То же **кнопкой «Назад»**, тапом по затемнению и **шевроном «⌄»** в шапке формы — результат тот же.
- [ ] Тап по полоске → форма развернулась, **набранное на месте**; дописать «Вид работ» → «Сохранить»
      → в журнале **одна** запись, не две (главная проверка сворачивания).
- [ ] Свернуть → развернуть → свернуть ещё раз → всё ещё **одна** запись.
- [ ] «×» на полоске → полоска ушла, запись осталась в журнале.
- [ ] Полоска **не закрывает** кнопку «+» и последнюю строку матрицы (обе видны над ней).
- [ ] Свернуть запись → нажать «+» → открылась ПУСТАЯ новая форма, полоска исчезла (не осталась
      с чужой подписью).
- [ ] Свернуть запись → долгий тап по ней в матрице → удалить → **полоска исчезла** (не разворачивается
      в вечную крутилку).
- [ ] Без выбранного подрядчика: ввести текст → свайп вниз → **лист возвращается**, «Подрядчик»
      подсвечен, полоски нет. Кнопка «Не сохранять» закрывает и ничего не пишет.
- [ ] Открыть форму и сразу свайпнуть вниз → **не появилось ни пустой строки, ни полоски**.
- [ ] Правка существующей записи: изменить текст → свайп вниз → правка сохранена, **фото не
      потерялось и не задвоилось** (проверить оба слота).
- [ ] Дважды быстро нажать «Назад» на новой записи → в журнале **одна** запись, не две.
- [ ] Кнопка «Сохранить» по-прежнему требует обязательные поля (подсветка «Вида работ») и закрывает
      форму **совсем**, без полоски.

## Цвета значений
- [ ] В форме у каждого поля справа **кружок**: пустой контур, пока значение не введено (и не
      нажимается), цветной — когда значение покрашено.
- [ ] Ввести «Штукатурка» в «Вид работ» → кружок → выбрать цвет → в матрице **подколонка «ВИД РАБОТ»
      этой ячейки залилась**, текст поверх заливки читается.
- [ ] Завести вторую запись с тем же «Штукатурка» → она покрасилась **сама**, без повторного выбора.
- [ ] Начать вводить «Шту» → в подсказках автодополнения у варианта виден **его кружок цвета**.
- [ ] Отдалить зум до яруса, где текст уже не читается (LOD1) → **цветные полосы видны** и работают
      как картина; отдалить до «картины месяца» (LOD2) → блоки по-прежнему **по подрядчику**.
- [ ] Вкладка «Список» → та же заливка у ячеек тех же значений.
- [ ] Кружок → «Без цвета» → заливка ушла везде разом.
- [ ] Тёмная тема: заливка не съедает текст ни в матрице, ни в списке.

## Форма записи — поля и подрядчики
- [ ] «Локация» в форме — **одна строка**, растягивается максимум до двух; «Вид работ» остаётся
      высоким блоком.
- [ ] В списке подрядчиков формы внизу «+ Создать нового подрядчика» → диалог → после «Сохранить»
      новый подрядчик **сразу выбран** в форме, виден в матрице и на экране «⋮ → Подрядчики».

## Поворот и восстановление состояния
- [ ] **Поворот экрана** на матрице/форме/лайтбоксе — данные и открытые листы не пропадают.
- [ ] **«Don't keep activities» ВКЛ**: пройти форму записи с фото, свернуть-развернуть — состояние цело.

## Данные и удаление — M7/M8
- [ ] **Идемпотентный импорт**: импортировать один и тот же `.psbackup` дважды → второй раз «обновлено/
      пропущено», дубликатов журналов/записей/фото нет.
- [ ] **Каскадное удаление журнала** (long-press карточки → подтвердить): журнал, его записи и фото
      исчезают; свободное место возвращается (файлы фото удалены).
- [ ] EXIF-поворот фото корректен на снимках с камеры (проверить на Samsung, если есть).

## Экспорт — M6
- [ ] xlsx открывается в Excel и Google Sheets: merged-шапка, freeze B3, фото на местах; CSV без
      кракозябр (кириллица) в русском Excel.
- [ ] **Фото видны именно в Google Sheets НА ТЕЛЕФОНЕ** — картинку, которой тесно в её ячейке, он не
      рисует вовсе (правило и замеры — docs/evolution.md, «xlsx»). Обязательно после любой правки
      размеров фото, высоты строк или ширины колонок «Ф».
- [ ] **Среди проверяемых фото обязательно есть ГОРИЗОНТАЛЬНОЕ.** Вертикальные проходят даже на
      сломанной вёрстке: поперёк колонки у них короткая сторона. Баг 2026-08-06 прожил до релиза
      именно потому, что глазами проверяли вертикальные.
- [ ] Проверять на файле, забранном из папки экспорта **до** открытия любым просмотрщиком:
      и Sheets, и Excel переписывают файл, и по переписанному о нашем выводе судить нельзя.

## Логи
- [ ] Меню ⋮ → «Отправить логи»: открывается системный выбор приложения, файл непустой — заголовок
      сессии (версия, Android, модель) и строки «экспорт начат/завершён» после пробного экспорта.
- [ ] Крэш-тест по желанию: убить процесс не обязательно, но после любого краша в логе должен быть
      стектрейс (uncaught handler), а приложение — штатно показать системный диалог, не зависнуть.

## M9 — проверено на реальном телефоне 2026-07-19

Обновление ставилось поверх сборки M8 (тот же ключ подписи), данные сохранялись.

- [x] **Миграции 1→5 на настоящих данных**: журнал, записи, фото, локации и виды работ
      пережили обновление. Это единственный способ проверить путь по-настоящему —
      androidTest гоняет его на выдуманных фикстурах.
- [x] Поля приходят из БД: в шапке Ф / Л / ВИД РАБОТ, редактор поля открывается и работает.
- [x] Вид «Список» с сортировкой по произвольному столбцу.
- [x] Длинный текст виден целиком в «Списке» — без обрезки.
- [x] Даты без года в матрице и списке.
- [x] Настройки таблицы доступны из меню «⋮» самого журнала.

Найдено на устройстве и не ловится тестами:

- [ ] **Колонки «Подрядчик» и «Л» в списке узкие** — имена рвутся посреди слова
      («Хамамщи/ки», «Плиточни/ки»).
- [ ] **Пустая полоса над шапкой списка.**
- [ ] **Автовысота раздувает строки матрицы**: одна длинная запись растягивает строку по всем
      колонкам, экран занимает пустота. Кандидат на потолок в 6–8 строк текста.

Осталось непроверенным (нужен живой жест или настоящий файл):

- [ ] Плавность пинч-зума и переход через порог LOD0↔LOD1.
- [ ] Задержка при открытии журнала (замер высот идёт синхронно в композиции).
- [ ] Предпросмотр печати xlsx в Excel: влезает ли текст, повторяется ли шапка.
- [ ] Импорт настоящего «Copy of Июнь.xlsx» — в эталоне вырезаны sharedStrings и фото,
      так что путь «фото из чужого файла попало в запись» вживую не гонялся.

## Логи и видимость фото в Sheets — проверено на реальном телефоне 2026-07-22/23

- [x] Файловый лог пишется с заголовком сессии; экспорт оставляет «экспорт начат/завершён»
      с числом записей и фото (снято с устройства через adb).
- [x] ~~Правило видимости: видно ⟺ картинка вылезает за ячейку ИЛИ длинная сторона ≥ ~52pt.~~
      **Вывод оказался неверным** — см. следующий раздел. Наблюдения (42pt нет, 52pt да) верны, но
      в тех файлах вместе с размером фото менялась и ширина колонки, так что «мелкое» и «тесно»
      были неразличимы, и объяснение выбрали не то.
- [ ] Кнопка «Отправить логи» руками на устройстве (шаринг-диалог) — ещё не прогонялась.

## Видимость фото в Sheets — переснято 2026-08-06

- [x] **Видно ⟺ картинке просторно в её ячейке-якоре**, запас ≥10px по ширине. Не «крупная» и не
      «вылезает» — ровно наоборот. Замеры: фото 74.67px в колонке 70px нет, 75px нет, 80px нет,
      85px да, 86px да; фото 132.74px в колонке 133px нет, 154px да; фото 99.6px в колонке 133px да.
      Разводит размер и простор последняя пара: фото одно и то же, меняется только колонка.
- [x] Продакшн-геометрия 1.3 (колонка 86px, фото 74.67px) подтверждена на файле, сгенерированном
      самим `XlsxWriter`: видны все четыре кадра, включая 1280×720 и 960×720.
- [ ] То же самое в **Microsoft Excel на телефоне** — прораб смотрит там, а правило может отличаться.
      На тестовом Nothing Phone Excel не установлен, проверка не гонялась.

### Как снимать такие замеры (adb, без рук)

Способ проверен и повторяем; один A/B-файл с двумя колонками разной ширины решает вопрос за минуту:

```bash
ADB=~/.local/opt/android-sdk/platform-tools/adb
$ADB push проба.xlsx /sdcard/Download/проба.xlsx
$ADB shell "am broadcast -a android.intent.action.MEDIA_SCANNER_SCAN_FILE -d file:///sdcard/Download/проба.xlsx"
ID=$($ADB shell "content query --uri content://media/external/file --projection _id \
     --where \"_display_name='проба.xlsx'\"" | grep -o '_id=[0-9]*' | cut -d= -f2 | tail -1)
$ADB shell "am force-stop com.google.android.apps.docs.editors.sheets"   # иначе покажет прошлый файл
$ADB shell "am start -a android.intent.action.VIEW --grant-read-uri-permission \
     -t application/vnd.openxmlformats-officedocument.spreadsheetml.sheet \
     -d content://media/external/file/$ID"
sleep 9 && $ADB exec-out screencap -p > снимок.png
```

- `file://`-интент не годится (`FileUriExposedException`) — только `content://` из MediaStore.
- Обе ветки A/B держать **в одном файле** разными колонками «Ф»: один снимок, никаких сомнений,
  что между запусками изменилось что-то ещё.
- Менять в пробе строго одну величину. Ошибка 2026-07-22 была именно в том, что меняли две.
